"""
애플리케이션 설정 모듈

이 모듈은 애플리케이션 전반에서 사용되는 설정값들을 중앙에서 관리합니다.
모든 하드코딩된 값들을 이 파일로 이동하여 유지보수성을 향상시킵니다.
"""

# ============================================================================
# OpenAI API 설정
# ============================================================================

# 기본 모델 이름
DEFAULT_MODEL: str = "gpt-4o-mini"

# 기본 temperature (일반 대화용)
DEFAULT_TEMPERATURE: float = 0.7

# 리서치 모드용 temperature (더 정확하고 구조화된 정보를 위해 낮게 설정)
RESEARCH_TEMPERATURE: float = 0.5

# 요약용 temperature (일관성을 위해 낮게 설정)
SUMMARY_TEMPERATURE: float = 0.3

# 최대 토큰 수
MAX_TOKENS: int = 1000

# 리서치 모드용 최대 토큰 수 (더 상세한 정보를 위해 더 많이 설정)
RESEARCH_MAX_TOKENS: int = 1500

# ============================================================================
# 재시도 및 백오프 설정
# ============================================================================

# 기본 재시도 횟수
MAX_RETRIES: int = 3

# 지수 백오프의 기본 대기 시간 (초)
BASE_BACKOFF_SECONDS: int = 2

# ============================================================================
# 파일 및 디렉토리 설정
# ============================================================================

# 대화 저장 디렉토리
DATA_DIR: str = "data"

# 파일명 타임스탬프 형식
SAVE_FORMAT: str = "%Y%m%d_%H%M%S"

# ============================================================================
# 대화 요약 설정
# ============================================================================

# 요약을 위한 최소 메시지 수
MIN_MESSAGES_FOR_SUMMARY: int = 3

# ============================================================================
# Tavily 검색 설정
# ============================================================================

# 기본 검색 결과 개수
TAVILY_DEFAULT_MAX_RESULTS: int = 5

# 기본 검색 깊이 (기본 모드: 빠르고 간단한 검색)
TAVILY_DEFAULT_SEARCH_DEPTH: str = "basic"

# 고급 검색 깊이 (더 상세하고 포괄적인 검색)
TAVILY_ADVANCED_SEARCH_DEPTH: str = "advanced"

# 검색 쿼리 최적화: 검색 시 제거할 한국어 표현 리스트
# 사용자 질문에서 불필요한 표현을 제거하여 검색 품질 향상
QUERY_REMOVE_PHRASES: list[str] = [
    "알려줘",
    "설명해줘",
    "찾아줘",
    "에 대해",
    "에 대해서",
    "에 관해서",
    "에 관해",
    "좀",
    "제발",
    "부탁해",
    "부탁드려",
]

# 검색 쿼리 최적화: 연도로 변환할 시간 표현 리스트
# 시간 관련 표현을 현재 연도로 변환하여 검색 정확도 향상
TIME_INDICATOR_PHRASES: list[str] = [
    "최신",
    "요즘",
    "현재",
    "올해",
    "이번 년도",
    "이번년도",
    "최근",
    "지금",
]